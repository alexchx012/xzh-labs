

## 计划：删除第二个卡片并重构实习经历板块

### 修改内容

**`src/components/ExperienceSection.tsx`**：

1. **删除第二个经历数据**（第16-26行的机器人开发实习生条目）
2. **重构布局**：只剩一个卡片后，移除时间线样式（竖线、圆点），改为单卡片居中展示，简化结构：
   - 去掉 `pl-8`、时间线竖线、圆点等元素
   - 保留 glass 卡片样式，单卡片直接居中显示
   - 去掉 `.map()` 循环，直接渲染单个卡片

